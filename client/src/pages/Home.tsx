/** Orbiting Archive design reminder: this asymmetric home route is an interactive constellation map, never a generic centered portfolio landing page. */
import { ConstellationHub } from "@/components/hub/ConstellationHub";
export default function Home(){return <ConstellationHub/>}
